let fs = require('fs');
const path = require('path');
let pathModule = require('path');

function helpFn() {
    console.log(`
    List of all commands 
    tree
    organize
    help`);
}
module.exports ={
    helpFunc : helpFn
}